name = "fanxiaoxing-bot"
main = "src/index.js"
compatibility_date = "2024-01-01"

# KV 命名空间绑定
# 请先创建 KV 命名空间，然后替换下面的 ID
[[kv_namespaces]]
binding = "AI_KV"
id = "your-kv-namespace-id"

# 环境变量（可选，推荐使用 Cloudflare Dashboard 设置）
# [vars]
# GLM_API_KEY = ""
